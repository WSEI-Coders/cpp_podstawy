#pragma once
/**
 * Plik nagłówkowy (main.h)
 * 
 * Opis:
 * - Użycie `#pragma once` zapobiega wielokrotnemu dołączaniu tego samego pliku nagłówkowego podczas kompilacji.
 * - Zawiera deklaracje, nagłówki oraz wszelkie definicje, które mają być współdzielone z plikiem źródłowym `.cpp`.
 */

#include <iostream>
#include <string> // Przykładowo dołączona biblioteka do pracy z tekstem
